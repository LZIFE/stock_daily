# Design Context

本文件供 [impeccable](https://impeccable.style) 设计语言读取，避免每次重做都重新询问语境。

## 受众

**唯一使用者是作者本人** —— 一个做个股基本面研究的个人投资者。
不是给客户看的、不是给团队用的、不对外发布。

## 用途

做**买入前的尽职调查**时，回答一个问题：**这只股票现在能不能碰？**

具体场景：
- 手上已有一只票，想快速看 29 位作者各自怎么看、分歧在哪
- 想确认「这个分数相对它自己的历史是高还是低」
- 想知道「规则看不到什么风险」（读公告与研报的批注）

**不是**用来做量化选股、不是用来看行情的、不是每天开盘前扫一遍的工具。

## 气质

**严肃、克制、可审计。**

这份界面的本质是一份**证据文件**，不是仪表盘。用户需要的是：
- 一眼分清「哪些是证据、哪些是推测」
- 每个数字都能追溯到规则原文
- 明确知道系统**不知道**什么（算不出来的书、缺失的字段、统计上不显著的结论）

**不要**把它做得像金融科技产品（发光、深色、霓虹、实时跳动）——
那会暗示一种它并不具备的确定性和时效性。

## 已确认的设计方向

**研究笔记 / 手稿，浅色。**

核心隐喻（同时承担诚实性表达，不是装饰）：
- **事实层 = 印刷正文**：墨色、楷体标题、细规线、定义列表
- **AI 层 = 红笔批注**：朱红虚线边框、边注栏标记、楷体标签、默认折叠

## 设计决策（已定，不要推翻）

| 项 | 决定 | 理由 |
|---|---|---|
| 底色 | 暖纸色 `oklch(96.8% 0.010 84)` | 不用纯白；中性色一律带暖色偏移 |
| 强调色 | 朱红 `#b23a1d`（唯一） | 批注色；用得越少越有力 |
| 展示字体 | 楷体（`Kaiti SC`，macOS 自带） | 手稿感；系统字体、零下载 |
| 数据字体 | 系统黑体 + `tabular-nums` | 可读性优先；**不用等宽字体** |
| 正文 | 16px / line-height 1.7 | impeccable 硬性要求：正文最小 16px |
| 布局 | 左侧边注栏（§编号）+ 竖直规线 + 正文 | **不用卡片**；靠规线、留白、定义列表建层级 |
| 判定三态 | 低饱和墨绿 / 赭黄 / 暖灰 | 像旧印章，不像交通灯 |
| 动效 | 仅载入错落上浮；实现 `prefers-reduced-motion` | impeccable 明确要求 |

## 必须避免（都是 AI 生成物的指纹）

- hero metric 模板：大数字 + 小标签 + 辅助统计 + 渐变强调
- 圆角元素配彩色粗侧条（`border-left: 3px`）
- 一切包进圆角卡片；卡片套卡片
- 渐变文字、玻璃拟态、发光边框
- 纯黑 `#000` / 纯白 `#fff` / 纯灰中性色
- 等宽字体当「技术感」的偷懒替代
- 深色底 + 霓虹强调（看起来「酷」但不需要做任何设计决策）

## 产品约束（设计不能违背）

1. **绝不显示排名**。核心分只有 199 个不同取值 / 4888 只，只显示判定 + 分位 + 并列只数。
2. **算不出来的书显示 N/A，不是 50**。视觉上必须能看出「缺失」与「中性」的区别。
3. **AI 层默认折叠**。未经验证的解读不该抢在证据之前影响判断。
4. **免责要跟着数字走**，不能只堆在页脚。
5. 关键结论必须带样本量、窗口、AUC —— 让用户能自己判断证据强度。
