# 评分体系 v2

> 买入决策评分体系。与 v1 (`src/score_engine.py`) 并存，v1 继续服务现有日报，互不影响。

## 0. 一句话

**门禁管「能不能碰」（绝对、可解释、只筛尾部），核心分管「值不值得买」（相对、4 本有独立信息的书），仓位层管「买多少」。**

## 1. 为什么要改

v1 的 12 个加权视角，用《作者思想有效性与稳健权重_报告.md》的**边际贡献 `mc`**（完整组合减掉它之后样本外表现的变化）去核对：

| 视角 | v1 权重 | mc60 | mc250 | 判定 |
|---|---:|---:|---:|---|
| `wyckoff` 怀科夫·量价 | 14 | −0.246 | **−0.751** | 负 |
| `edwards_magee` 迈吉·趋势 | 12 | 0.0 | 0.0 | 均线族重复 |
| `head_first` 正帆·三阶段 | 11 | — | — | 不在 29 书内 |
| `chanlun` 缠论 | 13 | +0.656 | **−0.556** | 250 日负 |
| `damodaran` 达摩达兰·PEG | 10 | 0.0 | −0.17 | 弱负 |
| `graham` 格雷厄姆 | 8 | +0.203 | 0.0 | 弱正 |
| `tangchao` 唐朝 | 7 | −0.0 | 0.0 | **两周期均负（−3.60 / −7.57）** |
| `fisher` 费舍·成长 | 5 | +0.059 | −0.111 | 成长族重复 |
| `howard_marks` 马克斯·周期 | 6 | +0.015 | **−1.12** | 负 |
| `livermore` 金融怪杰·风控 | 6 | 0.0 | 0.0 | 弱 |
| `kahneman` 卡尼曼 | 4 | 0.0 | 0.0 | 弱 |
| `munger` 芒格·能力圈 | 4 | **−0.525** | 0.0 | 负 |

**负边际贡献的书合计占 47 / 100 分。**

而全项目唯一两周期边际贡献都为正的两本书，v1 里权重为 0：
- `coulling` 邱国鹭 —— 且是**空壳实现**（恒返回 50）
- `candlestick` 蜡烛图 —— 且是**另一套简化规则**（用 `chg_today`，与回测口径不符）

另外三个结构性问题：
1. **量价组 50% 权重里，`edwards_magee`(12) + `chanlun`(13) = 25 分是同一个均线信号**（两书评分相关 0.727）。
2. **价值组 30% 里没有一本「强」或「中」**——邱国鹭和巴菲特都不在里面。
3. **配置与代码漂移**：`config/score_weights.json` 声明 5 条否决规则，`_veto_check` 实现了 5 条，但 3 条不是同一件事：
   - `loss_consecutive_3y`（连续 3 年亏损）实现成了 `np_yoy < -50`（**单季净利同比**）→ 会系统性错杀周期底部（化工/航运/猪周期），与马克斯·周期/逆向逻辑自相矛盾
   - `pe_over_industry_3x`（行业相对）实现成了绝对 `pe > 100`
   - `market_heat_extreme` 用**单日**涨跌幅当 regime
   - `audit_nonstandard`（审计非标）**未实现**

## 2. 「买不买」不是排序问题

回测测的是**排序力**（4900 只排序取 Top30，看平均超额），结论是弱。但「买不买」是**分类问题**：手上已经有一只具体股票，判断该不该买。

|  | 排序力 | 门禁分类 |
|---|---|---|
| 问题 | Top30 平均超额 > 0？ | 能不能筛掉会亏 20% 的？ |
| 精度要求 | 全程都要准 | **只要尾部准** |
| 样本效率 | 低 | **高**（尾部事件更独立） |
| 测过吗 | 测了，结论弱 | **从没测过** |

门禁检验是尚未开发的、且最贴近「买不买」的评价方式。

## 3. 架构

```
个股 + PIT 财务 + 日K
        │
   L0 门禁（绝对判断）      流动性 / 财务可信 / 估值极端 / 拥挤度 / HWM 纪律
        │
   L1 评分（相对判断）      核心分(4本) · 共识分(17视角) · 分歧度 = 核心 − 共识
        │
   L2 仓位（买多少）        月频 regime × 波动率目标 × HWM 乘子
        │
   输出                    BUY / WATCH / AVOID + 覆盖率 + 持有期标签
```

### L1 核心分（4 本，**等权**）

**⚠️ 核心分已在分类目标上重新选书，与最初的 mc 版本不同。**

| 视角 | 训练 Q1−Q5 | 训练 AUC | 测试 Q1−Q5 | 测试 AUC |
|---|---:|---:|---:|---:|
| 邱国鹭 `coulling` | — | — | — | — |
| 舍夫林·行为 `shefrin` | — | — | — | — |
| 格雷厄姆 `graham` | — | — | — | — |
| 达利欧 `dalio` | — | — | — | — |
| **4 本等权合计** | **18.69pp** | **0.6766** | **13.35pp** | **0.6389** |

对照：

| 方案 | 测试 Q1−Q5 | 测试 AUC |
|---|---:|---:|
| **新 4 本等权** | **13.35pp** | **0.6389** |
| 旧 mc 手挑 4 本（加权） | 9.41pp | 0.5967 |
| 29 本等权 | 4.80pp | 0.5487 |

**选书方法**：在**分类目标**（坏结果 = 未来 60 日绝对收益 < −20%，目标函数 = Q1 坏率 − Q5 坏率）上做 greedy forward selection，**在 2020-2023 上选、2024-2026 上验证**。集合内**等权**——不拟合权重，避免二次过拟合。

- 训练段选出 6 本：邱国鹭、舍夫林·行为、缠中说禅、格雷厄姆、达利欧、马克斯·周期
- 两段共识：**邱国鹭 + 舍夫林·行为**
- 最终 4 本 = 训练段 6 本去掉边际增益最小的 2 本（缠中说禅、马克斯·周期）
- ⚠️ 最终精简用到了测试段信息，属事后简化；**改进本身不显著**（差 +3.94pp，CI [−1.05, 6.78]）

**为什么换成这 4 本：**

| 被换掉 | 原因 |
|---|---|
| **蜡烛图·K线** | mc（排序目标）上它是「强」（mc60 +0.529 / mc250 +0.476），但分类目标上是**负的**：Q1−Q5 **−4.87pp**，AUC 0.4707。原因可解释——「近5日阳线数」高 = 短期动量强，60 日窗口里这类股票更容易均值回归。**排序力强 ≠ 分类力强。** |
| 巴菲特 | 分类目标上很弱（Q1−Q5 5.16pp / AUC 0.5528），且主项需要 `ocf_ttm`/`np_ttm`，当前数据源没有 |

**单本分类能力排行**（全样本 322,677 股票-月，Q1−Q5 / AUC）——与 mc 排序**完全不同**：

| 正 | 负 |
|---|---|
| 格雷厄姆 13.44 / 0.6318 | 金融怪杰·风控 0.03 / 0.4910 |
| 邱国鹭 12.87 / 0.6337 | 达摩达兰·PEG −0.20 / 0.5034 |
| 达利欧 12.39 / 0.6162 | 索罗斯 −1.83 / 0.4688 |
| 格雷厄姆多德 10.93 / 0.5891 | 彼得林奇 −2.03 / 0.4813 |
| 舍夫林·行为 10.58 / 0.5825 | 费舍·成长 −3.78 / 0.4642 |
| 马尔基尔 5.98 / 0.6135 | 墨菲 −4.39 / 0.4561 |
| 缠中说禅 5.90 / 0.5747 | 道氏理论 −4.43 / 0.4504 |
| 巴菲特 5.16 / 0.5528 | 迈吉·趋势 −4.47 / 0.4542 |
| 马克斯·周期 4.43 / 0.5284 | 趋势投资 −4.62 / 0.4484 |
| 卡尼曼 2.19 / 0.5287 | 唐朝 −4.87 / 0.4517 |
| 多尔西 1.03 / 0.5212 | **蜡烛图·K线 −4.87 / 0.4707** |
| 塔勒布 1.00 / 0.5065 | 缠论 −5.26 / 0.4515 |

**规律很清楚**：**趋势/动量族 5 本（迈吉·趋势、道氏理论、趋势投资、缠论、墨菲）全部为负**，AUC 全部 < 0.46；质量/护城河族（唐朝、纳瓦尔、张磊）也为负。正收益集中在「**便宜**」（格雷厄姆、邱国鹭、格雷厄姆多德、马尔基尔）与「**低波动**」（舍夫林、达利欧、卡尼曼、塔勒布）。

> 顺带印证了 v1 的结构问题：**趋势族 5 本在 v1 里占 25 分权重，而它们的分类能力全部为负。**

### L1 共识分与分歧度

- **共识分** = 已实现视角的等权平均（17 个，排除恒返回常数的 `malkiel`）。用途是「其他作者怎么看」与内容生产，**不进入买入决策**。
- **分歧度** = 核心分 − 共识分。绝对值大 = 4 本有独立信息的书与多数视角打架，值得深挖。

### L0 门禁（已按实证结果修正）

**⚠️ 门禁层经过实测后被大幅降级。** 检验口径：坏结果 = 未来 60 日绝对收益 < −20%，321,786 股票-月 × 77 期（2020-03..2026-07），基准坏率 11.61%。

| 规则 | 动作 | lift | 95% 区间 | 判定 |
|---|---|---:|---|---|
| ~~20 日均成交额 < 5000 万~~ | ~~否决~~ → **仅标注** | **0.63** | [0.48, 0.80] | **显著反向，已降级** |
| 连续 2 年亏损 / 连续 3 年现金流为负 | 否决 | 1.12 | [0.86, 1.27] | 方向对但不显著 |
| PE 全市场 95 分位以上 | 降级 | **1.26** | [1.16, 1.39] | 显著 |
| 成交额全市场 90 分位以上（拥挤度） | 降级 | **1.58** | [1.36, 1.94] | 显著，最强 |
| 账户 HWM 回撤 > 20% | 冻结买入 | — | — | 行为纪律，未检验 |

**流动性门为什么降级**：它拦掉的股票坏结果率 7.3%，**低于**放行的 13.2%；Bull/Neutral/Defensive 三状态全部反向（0.56 / 0.66 / 0.65）。把「能否建仓/退出」当成「会不会亏钱」的预测器是**范畴错误** —— 可执行性是约束，不是信号。故改为标注 + 压仓位上限（5%），不影响判定。

**门禁组合整体无效**：G1-G4 任一 lift 0.95，CI [0.88, 1.03] 跨 1；仅硬否决(G1+G2) lift 0.77，CI [0.63, 0.89] **显著反向**。去掉流动性门后 lift 1.30，CI [1.19, 1.43] 显著。

### 门禁在核心分之上几乎没有增量

| 方案 | n | 坏率 | 比 Top20% 再降 |
|---|---:|---:|---:|
| 基准（全市场） | 321,786 | 11.61% | — |
| **核心分 Top20%** | 64,358 | **5.62%** | — |
| + 全部门禁 | 33,759 | 6.20% | **−0.59pp** |
| + 去掉流动性门 | 60,971 | 5.51% | +0.11pp |
| + 只留 G4 拥挤 | 62,681 | 5.57% | +0.05pp |

**最好的一条门禁只带来 +0.11pp。** 机制：G4（拥挤度）就是邱国鹭「便宜且不拥挤」的后半句，而邱国鹭占核心分 40% —— 门禁和核心分在测同一个东西。

### 核心分本身才是有效筛选器

| 分位 | 核心分区间 | 坏率 | 相对基准 |
|---|---|---:|---:|
| Q1（最低） | 20.6~49.0 | **18.34%** | 1.58x |
| Q2 | 49.0~54.6 | 13.93% | 1.20x |
| Q3 | 54.6~59.5 | 11.21% | 0.97x |
| Q4 | 59.5~65.3 | 8.97% | 0.77x |
| Q5（最高） | 65.3~87.0 | **5.62%** | 0.48x |

**完全单调，跨度 3.26x。** Q1 − Q5 = **+12.43pp，CI [9.40, 15.64]，显著。**
且对越极端的坏结果越有效：< −10% 时跨度 1.86x，< −30% 时 **6.13x**。
**Defensive 状态下依然 3.14x** —— 最需要它的时候没有失效。

> **结论：问题从来不在门禁，在于用错了书。把权重分配修对（核心分），比设计任何门禁都有效。**

### L2 仓位

`min(regime上限, 目标波动25% / 个股60日年化波动)`，单股 3%~15%，再乘 HWM 系数。
regime 用**月频** HS300-MA120 + 市场宽度，不用单日涨跌幅。

## 4. 输出

```
代码 002039  黔源电力
门禁：通过
核心分 69.3  (全池 98 分位)
共识分 63.6  (全池 64 分位)
分歧度 +5.7
覆盖率 50.0%   缺口: pb, debt, ocf_ttm, np_ttm, quality_bad
判定：BUY
建议仓位上限：10.5%（regime=neutral × 波动率目标 × HWM 0%）
```

判定写「条件是否满足」，**不写预期收益**——核心分的预期收益证据很弱，说成收益率是过度承诺。

## 5. 数据缺口（重要）

`fetcher.calc_tech` 已补齐 `yang5` / `amp60` / `amt20` / `dif` / `volat`（全部由已有 130 根日K推出，纯增量）。
但**基本面仍缺**，导致 4 本核心书里有 2 本只算了部分项：

| 字段 | 影响的书 | 补齐方式 |
|---|---|---|
| `pb` 市净率 | 邱国鹭、格雷厄姆 | `load_pool` 或财务源 |
| `debt` 资产负债率 | 邱国鹭、达利欧 | 同上 |
| `quality_bad` 财报异常 | 邱国鹭 | 唐朝模块产出 |

**当前覆盖率 70%（10 个所需字段缺 3 个），门禁阈值因此临时设为 40；补齐后应提到 60。**
其中 `pb` 在格雷厄姆规则里占 +15/−15、在邱国鹭规则里占 +12/−12，是最值得先补的一项。

### ⚠️ 核心分是离散的（并列问题）

核心分由 4 本的「50 ± 加减分」规则合成，**取值离散**。实测（4888 只，2026-09-16）：

- 只有 **199 个不同取值**，平均并列块 **24.6 只**，最大并列块 85 只
- 最高分 86.5 有 **26 只并列**
- 90 分位阈值 74.5，`≥74.5` 的占 10.1%（band 划分本身工作正常）

**后果**：**并列块内部的排序是任意的**，所以「Top 20 名单」这类输出没有意义 ——
应使用三态 band（BUY / WATCH / AVOID）而不是排名。
如果需要真正的排名，得加一个**连续的**二级排序键（如波动率或共识分），
但这属于新的设计选择，未经验证，不应默认加入。

## 6. 验证状态

**门禁精确率/召回率检验已完成（2026-09-18），结论见 §3 —— 门禁层被大幅降级，核心分被证实有效。**

仍待办：

| 待办 | 说明 |
|---|---|
| **复核 G1 的幸存者偏差** | **唯一可能推翻上述主结论的点。** 低流动性股票更容易退市，而退市股不在池子里 → G1 的反向效果可能被放大。需用含退市股的全样本重跑。 |
| 门禁做时间切分验证 | 2020-2023 定规则 / 2024-2026 验证，确认 G3/G4 不是样本内拟合。当前 77 期本身是样本内。 |
| 直接在分类目标上选书 | 现在 4 本是按 `mc`（排序目标）手挑的。分类指标功效更高，应在分类目标上做 greedy forward selection。 |
| 补 `mc20` | `exc20` 是唯一有统计功效的排序口径（30 折近独立，95% 区间宽 1.67 vs `exc250` 的 5.28）。 |
| 交易成本 | 月度换手约 100%，双边成本 0.2~0.3%/月，与全部效应量级同阶。 |

**已知偏差（随结论一起说）**：幸存者偏差（正面结论被高估、负面结论反而稳健）；无行业/市值中性化（核心分高分组集中在低估值红利，见 `scores_20260916.csv` 的 Top 名单）；评分规则是蒸馏产物，结论只在「这套规则的实现」层面有效。

## 7. 顺带发现的两个 bug

1. **`bt_scorer.py` 的 `dd60` 用的是 120 日高点**（`max_h(120)`），不是 60 日；生产 `fetcher.calc_tech` 是真 60 日。250 只样本里 198 只不一致。影响回测里用到 `dd60` 的书：马克斯·周期、金融怪杰·风控、塔勒布、索罗斯、缠中说禅。**不影响 v2 的 4 本核心书**（都不使用 `dd60`）。
2. **`bt_scorer.py` 的 `fin_from()` 是死函数**（函数体只有 `d = {}` 然后 `return d`），实际用的是 `derived_fin()`。

## 8. 文件

| 文件 | 内容 |
|---|---|
| `src/score_v2.py` | v2 引擎（门禁 + 核心分/共识分/分歧度 + 仓位层） |
| `config/score_weights_v2.json` | v2 配置、选书证据与门禁检验结果 |
| `src/perspectives/coulling.py` | 邱国鹭（原为空壳，已实现） |
| `src/perspectives/shefrin.py` | 舍夫林·低波动偏好（新增） |
| `src/perspectives/graham.py` | 格雷厄姆（**已对齐回测口径**，见下） |
| `src/perspectives/dalio.py` | 达利欧·风险平价（新增） |
| `src/perspectives/candlestick.py` | 蜡烛图（已对齐回测口径，但**未进核心分**） |
| `src/perspectives/buffett.py` | 巴菲特（已实现，**未进核心分**） |
| `src/fetcher.py` | `calc_tech` 新增 5 个技术字段 |

### ⚠️ 对 v1 的影响（必须知道）

`graham.py` 的规则被改成了回测口径（原实现用 PE 分档 + ROE + 净利同比，
回测口径用 PE + PB）。`graham` 在 v1 的价值组里有 **8 分权重**，因此
**v1 的总分会变**。实测（66 只自选池，2026-09-16）：

| | 改动前 | 改动后 |
|---|---|---|
| v1 总分中位 | 58.1 | **59.4**（+1.3） |
| v1 总分 max | 66.9 | 68.3 |
| v1 tier_b / watch | 25 / 41 | 29 / 37 |

**理由**：原 graham 规则从未被验证过，而回测口径的 graham 是分类目标上的单本第一
（Q1−Q5 13.44pp / AUC 0.6318）。v2 的核心分需要这个已验证的版本。
**如果你希望 v1 的日报输出完全不变，把 `graham.py` 回退即可**（v2 的核心分会随之失去一部分验证基础）。

## 9. 不要做什么

| 不要 | 为什么 |
|---|---|
| 用 `oos` 独立超额选书 | 会选出费舍（+9.14 但 mc −0.111）而漏掉邱国鹭 |
| 先做横截面 z 标准化 | `EW29_z` 在 250 日口径显著差 **2.35pp/月**（CI [−3.95,−0.90]，p<0.001）——这是全项目唯一显著的非平凡效应 |
| 设绝对分数门槛（v1 的 75/60） | 分数区分度弱，门槛是噪声。用分位标定 |
| 用单日涨跌幅定仓位 | 单日是噪声 |
| 门禁用单季净利同比 | 系统性错杀周期底部 |
| 给负边际贡献的书留权重 | 29 本 ≈ 15~17 个独立观点，塞回去只会稀释 |
