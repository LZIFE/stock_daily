"""AI 解读层。

职责边界（不可逾越）：
    本层只做「解释」。判定、分数、分位、坏率一律由确定性引擎给出，
    模型的任何输出都不得改写它们 —— 见 guard.py 的服务端强制覆盖。

实测依据（2026-09-18，Agnes agnes-2.5-flash）：
    - 模型会输出非法 JSON：字符串内含裸引号、原始换行、``` 围栏 → repair_json 三层修复
    - 延迟波动极大：12.5s / 22s / 25s / 56s / >75s → 超时须 ≥120s，且必须异步
    - 注入攻击下模型**不会**输出「建议买入」，但**会**把公告里的虚假数字当事实复述
      → 复述无法靠 prompt 杜绝，只能靠来源标注 + 不进入判定 + UI 标注「未经独立核实」
"""
